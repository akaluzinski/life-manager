apiVersion: v1
kind: Service
metadata:
  name: {{ include "life-manager-cluster.fullname" . }}
  labels:
    {{- include "life-manager-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "life-manager-cluster.selectorLabels" . | nindent 4 }}
